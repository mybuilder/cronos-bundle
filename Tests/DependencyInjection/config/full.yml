my_builder_cronos:
    exporter:
        key: test
        mailto: config-test@example.com
        path: /bin:/usr/local/bin
        executor: php
        console: app/console
        shell: /bin/bash
